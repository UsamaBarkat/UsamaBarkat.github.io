# Spec-Driven Development working docs — kept local, never deployed.
# The repo root is served publicly by Vercel, so anything committed here
# is readable at https://usamanizamani.vercel.app/<filename>.
research.md
spec.md
plan.md

# Project memory for Claude Code. Kept local: it was being served at
# /CLAUDE.md, and it is a working note rather than site content.
CLAUDE.md

# Claude Code local configuration and permissions
.claude/
